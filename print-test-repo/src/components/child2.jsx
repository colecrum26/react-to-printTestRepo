import { forwardRef } from "react"

// const MyP = forwardRef(function MyP(props, ref) 
export default function MyP()
{
    return (
        <div>
        <p>This is some sample text.</p>
        <p>This is more text.</p>
        </div>
    );
};

// export default MyP;